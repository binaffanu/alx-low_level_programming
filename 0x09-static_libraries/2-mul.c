#include"main.h"

/**
 * mul - function ti multiply two integers
 * @a : first int to be multiplied
 * @b : second int to be multiplied
 * Return: return 0 on success
 */
int mul(int a, int b)
{
        return (a * b);
}
